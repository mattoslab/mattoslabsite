<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>The Mattos Lab</title>
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css" rel="stylesheet">
    <link href="static/css/style.css" rel="stylesheet">
  </head>
  <body>
    <header>
      <div class="container">
        <div class="page-header">
          {% if active_page.title_pic %}
          <img class="pull-right" src="static/images/{{ active_page.title_pic }}"/>
          {% endif %}
          <h1>The Mattos Lab <small>{% block subheader %}{{ active_page.title }}{% endblock %}</small></h1>
        </div>
      </div>
    </header>
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <ul class="nav navbar-nav">
          {% for page in pages -%}
          <li {% if active_page.href == page.href %}class="active"{% endif %}>
            <a href="{{ page.href }}">{{ page.title }}</a>
          </li>
          {%- endfor %}
        </ul>
      </div>
    </nav>
    <div class="container">
      {% block content %}
      {% endblock %}
    </div>
    <div class="false-footer">
    </div>
    <footer>
      <div class="container">
        contact info, etc.
      </div>
    </footer>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.1/js/bootstrap.min.js"></script>
  </body>
</html>
